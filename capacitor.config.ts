import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tejas.smartpiggybank',
  appName: 'SmartPiggyBank',
  webDir: 'dist/public'
};

export default config;
